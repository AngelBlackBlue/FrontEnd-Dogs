// import React from 'react';
// import { render } from '@testing-library/react';
// import SearchBar from '../components/SearchBar/SearchBar.jsx';

// test('Renderiza el componente SearchBar', () => {
//     // Renderiza el componente
//     const { getByPlaceholderText, getByText } = render(<SearchBar />);

//     // Comprueba que el input y el botón se rendericen
//     const inputElement = getByPlaceholderText('Buscar por raza....');
//     const buttonElement = getByText('Buscar');

//     // Asegúrate de que ambos elementos existan
//     expect(inputElement).toBeInTheDocument();
//     expect(buttonElement).toBeInTheDocument();
// });
